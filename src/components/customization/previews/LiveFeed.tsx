import React from 'react';
import { Feed as LiveFeed } from '../../../pages/member/Feed';

interface LiveFeedPreviewProps {
  styles: any;
  mode: string;
}

export function LiveFeed() {
  return <LiveFeed />;
}